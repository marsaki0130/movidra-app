.container
  = form_with(model: @comment, url: movie_comments_path(@comment.movie), local: true) do |f|
    %div 
      = f.label :feelings, 'コメント'
    %div
      = f.text_area :feelings
    = f.submit '保存', class: 'btn-primary'